#include <stdio.h>
#include <conio.h>
/*
Kiểm tra một số nhập từ bàn phím là số chẳn hay số lẻ.
*/
int main()
{
	int n;
	printf("Enter an integer: ");
	scanf("%d",&n);
	
		if(n%2==0)
		{
		printf("\n%d is even",n);
		}
		else
		{
		printf("\n%d is odd",n);
		}
getch();
return 0;
}
